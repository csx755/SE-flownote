{
  "name": "flownote",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"pnpm --filter @flownote/api dev\" \"pnpm --filter @flownote/web dev\"",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "benchmark:api": "pnpm --filter @flownote/api benchmark",
    "benchmark:api:1000qps": "pnpm --filter @flownote/api benchmark:1000qps",
    "db:push": "pnpm --filter @flownote/api db:push",
    "db:studio": "pnpm --filter @flownote/api db:studio"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
